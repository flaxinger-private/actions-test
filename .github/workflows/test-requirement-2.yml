name: test print
on:
  pull_request:
    types: [ opened, reopened, synchronize, labeled, unlabeled, closed ]
    paths:
      - 'b'
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: exit 1

